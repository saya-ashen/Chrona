// ⚠️ Deprecated — schemas have moved to @chrona/contracts/api.
// These re-exports exist for backward compatibility with any code
// that still imports from this file. Remove after all consumers migrate.
export {
  createAiClientSchema,
  taskWorkspaceChatSchema,
} from "@chrona/contracts/api";
