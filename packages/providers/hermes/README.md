# Hermes provider scaffolding

This folder mirrors the OpenClaw provider layout:

- `bridge/`
- `integration/`
- `plugin-structured-result/`

Add Hermes-specific packages here when Hermes backend support is implemented.
