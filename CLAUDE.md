# Chrona

Project rules and codebase orientation live in [`AGENTS.md`](AGENTS.md) — read
it first. It links the curated docs for understanding the system
([`docs/architecture.md`](docs/architecture.md)), where code belongs
([`docs/package-boundaries.md`](docs/package-boundaries.md)), and the frontend
([`docs/frontend-structure.md`](docs/frontend-structure.md)), plus the
mandatory tool-routing and frontend conventions.

For structural code questions (callers, definitions, blast radius), prefer the
codegraph / GitNexus MCP tools over grep — see `AGENTS.md`.

## Agent skills

### Issue tracker

Issues and PRDs are tracked in `saya-ashen/Chrona` GitHub Issues. See `docs/agents/issue-tracker.md`.

### Domain docs

Domain documentation uses a multi-context layout. See `docs/agents/domain.md`.
